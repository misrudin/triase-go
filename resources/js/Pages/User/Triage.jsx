import React from "react";

const Triage = () => {
    return (
        <>
            <h1>Test</h1>
        </>
    );
};

// Triage.layout = (page) => <AuthLayout children={page} />;

export default Triage;
